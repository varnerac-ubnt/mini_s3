-define(SEND_REQ, ibrowse:send_req(URI,
                                   Method,
                                   Headers,
                                   Body,
                                   [{response_format, binary}])
       ).
-define(START_HTTPC, {ok, _} = ibrowse:start()).


